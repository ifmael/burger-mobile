overwrite: true
schema: "src/graphql/schema.graphql"
generates:
    src/graphql/models.ts:
        plugins:
            - "typescript"
            - "typescript-operations"
            - "typescript-react-apollo"
    # ./graphql.schema.json:
    #     plugins:
    #         - "introspection"
