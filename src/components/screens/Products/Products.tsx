import { Text } from "react-native";

import { ProductPops } from "../../navigation/navigation.types";

export default function Products({ route }: ProductPops) {
    // const x = route.params;
    return <Text>Product</Text>;
}
